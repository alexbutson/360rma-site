{
  "name": "rma-oauth",
  "version": "1.0.0",
  "private": true,
  "description": "GitHub OAuth code->token exchange for the 360rma-site admin editor",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "secret": "wrangler secret put GITHUB_CLIENT_SECRET"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
